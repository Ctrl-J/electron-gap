{
  "name": "electron-gap",
  "version": "0.0.1",
  "description": "ElectronGap.com website",
  "main": "server.js",
  "scripts": {
    "babel-node": "babel-node --stage 0",
    "start": "nodemon --exec npm run babel-node -- ./server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "BSD-3-Clause",
  "devDependencies": {
    "eslint": "^1.3.1",
    "nodemon": "^1.4.1"
  },
  "dependencies": {
    "babel": "^5.8.23"
  }
}
